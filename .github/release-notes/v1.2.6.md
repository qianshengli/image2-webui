## 更新内容

- 新增基于 GitHub Container Registry 的 Docker 发布链路，发版后可直接拉取镜像部署。
- 修正 Docker 镜像构建与运行目录，适配当前 `web/dist` 与 `/app/data` 结构。
- `docker-compose.yml` 已支持直接使用云端镜像并保留本地 build 能力。
- 可通过 `docker compose pull` + `docker compose up -d --remove-orphans` 直接完成升级。
- `README` 补充 Docker 部署与升级说明。
